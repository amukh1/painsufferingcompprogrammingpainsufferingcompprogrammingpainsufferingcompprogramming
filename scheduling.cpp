#include "bits/stdc++.h"

int main() {
    int n;
    std::cin >> n;

    std::vector<std::tuple<int,int>> times;

    for(int i = 0; i<n; i++) {
        int a,b;
        std::cin >> a;
        std::cin >> b;

        std::tuple<int,int> x = std::make_tuple(a,b);
    }
}